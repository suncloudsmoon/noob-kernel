#include "include/debugmode.hpp"

// The entry function must be called noob_start()

/*
Rules:
- Except for some definitions, the C++ standard lib should not be used
- When the C++ standard lib is used, a comment must follow it
- 
*/

extern "C" {
    void noob_start() {
        int a;
    }
}

